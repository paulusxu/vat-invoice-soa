<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lenovo.invoice.dao.MemberVatInvoiceMapper" >
  <resultMap id="BaseResultMap" type="com.lenovo.invoice.domain.MemberVatInvoice" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="Id" property="id" jdbcType="BIGINT" />
    <result column="InvoiceInfoId" property="invoiceinfoid" jdbcType="BIGINT" />
    <result column="LenovoID" property="lenovoid" jdbcType="VARCHAR" />
    <result column="CreateTime" property="createtime" jdbcType="TIMESTAMP" />
    <result column="ShopId" property="shopid" jdbcType="INTEGER" />
    <result column="faid" property="faid"/>
    <result column="storesid" property="storesid"/>
    <result column="type" property="type"/>
  </resultMap>

  <sql id="Base_Column_List" >
    Id, InvoiceInfoId, LenovoID,ShopId, CreateTime,faid,storesid,type
  </sql>


  <select id="getMemberVatInvoiceByLenovoId" resultMap="BaseResultMap" >
    select
    <include refid="Base_Column_List" />
    from membervatinvoice
    where LenovoID = #{lenovoId,jdbcType=VARCHAR} and type=#{type}
    <if test="storesid!=null and storesid!=''">
      and  storesid=#{storesid}
    </if>
    <if test="storesid==null or storesid==''">
      and storesid is NULL
    </if>
    ORDER by CreateTime DESC
  </select>

  <select id="getMemberVatInvoiceByZid" resultMap="BaseResultMap" >
    select <include refid="Base_Column_List" /> from membervatinvoice where InvoiceInfoId = #{zid}
  </select>

  <select id="getMemberVatInvoice" resultMap="BaseResultMap" >
    select
    <include refid="Base_Column_List" />
    from membervatinvoice
    where LenovoID = #{lenovoId,jdbcType=VARCHAR} and InvoiceInfoId=#{id,jdbcType=BIGINT}
  </select>

  <delete id="deleteMemberVatInvoice" parameterType="java.lang.Long">
    DELETE from membervatinvoice WHERE InvoiceInfoId=#{zid}
  </delete>

  <select id="getAllMemberVatInvoice" resultMap="BaseResultMap" >
    select
    <include refid="Base_Column_List" />
    from membervatinvoice
  </select>

  <select id="getNewZid" resultType="java.lang.Long">
    SELECT DISTINCT(new1) FROM moveorder
  </select>

  <select id="getMappingZid" parameterType="java.lang.Long" resultType="java.lang.Long">
    SELECT old1 FROM moveorder WHERE new1=#{zid}
  </select>
  
  <insert id="insertMemberVatInvoice" parameterType="com.lenovo.invoice.domain.MemberVatInvoice" >

    <selectKey resultType="java.lang.Long" keyProperty="id" order="AFTER">
      SELECT @@IDENTITY
    </selectKey>

    insert into membervatinvoice (InvoiceInfoId, LenovoID,ShopId,
      CreateTime,type,faid,storesid)
    values (#{invoiceinfoid,jdbcType=BIGINT}, #{lenovoid,jdbcType=VARCHAR},#{shopid},
      now(),#{type},#{faid},#{storesid})
  </insert>


  <update id="updateVatInvoice">
      UPDATE vatinvoice set storesid=#{storesid} WHERE id=#{id} and storesid is null;
      UPDATE membervatinvoice set storesid=#{storesid} WHERE InvoiceInfoId=#{id} and LenovoID=#{lenovoId} and storesid is null
  </update>

  <update id="delVatInvoice">
    DELETE FROM vatinvoice  WHERE id=#{id};
    DELETE FROM  membervatinvoice  WHERE InvoiceInfoId=#{id}
  </update>
</mapper>